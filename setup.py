from setuptools import setup

setup(name='divvy',
      version='0.2',
      description='',
      url='https://github.com/PhilippeMorere/divvy/',
      author='PhilippeMorere',
      author_email='',
      license='MIT',
      packages=['divvy'],
      scripts = ['divvy/divvy'],
      install_requires=[],
      zip_safe=False)
